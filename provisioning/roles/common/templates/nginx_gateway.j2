upstream auth_gateway {
    server 127.0.0.1:9000;
}

upstream auth_persona_gateway {
    server 127.0.0.1:9000;
}

server {
    server_name localhost;

    listen 9070;

    location / {
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Host $http_host;
      proxy_set_header X-NginX-Proxy true;

      access_log /var/log/nginx/auth_access.log;
      proxy_pass http://auth_gateway;
      proxy_redirect off;
    }
}

server {
    server_name localhost;

    listen 9070;

    location / {
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Host $http_host;
      proxy_set_header X-NginX-Proxy true;

      access_log /var/log/nginx/auth_persona_access.log;
      proxy_pass http://auth_persona_gateway;
      proxy_redirect off;
    }
}
